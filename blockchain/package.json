{
    "name": "futurelock-blockchain",
    "version": "1.0.0",
    "description": "Smart contracts for the FutureLock Time-Locked Intelligence Market",
    "main": "index.js",
    "scripts": {
        "compile": "npx hardhat compile",
        "test": "npx hardhat test",
        "deploy:localhost": "npx hardhat run scripts/deploy.js --network localhost",
        "node": "npx hardhat node"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^7.0.0",
        "hardhat": "^3.2.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0",
        "dotenv": "^16.4.0",
        "ethers": "^6.11.0"
    }
}
